# Keep the existing Proxmox system entries for root, EFI, swap and /proc.
# Add the following entries for the homelab data and backup disks.

UUID=YOUR_DATA_DISK_UUID /mnt/pve-data ext4 defaults,nofail 0 2
UUID=YOUR_BACKUP_DISK_UUID /mnt/pve-backup ext4 defaults,nofail 0 2
